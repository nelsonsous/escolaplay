{
  "name": "escolaplay",
  "version": "1.0.0",
  "private": true,
  "description": "EscolaPlay — app educativa (build pipeline)",
  "scripts": {
    "build": "node build.mjs",
    "build:report": "node build.mjs --report"
  },
  "devDependencies": {
    "csso": "^5.0.5",
    "html-minifier-terser": "^7.2.0",
    "terser": "^5.51.2"
  },
  "dependencies": {
    "playwright-core": "^1.62.1"
  }
}
